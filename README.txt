1.	Open the main folder in VS Code and open terminal and run the command:
	“pip install -r requirements.txt”. This will install all the project requirements.
2.	Run the Application with the command, “python app.py”.
3.	Access the website on localhost @ https://127.0.0.1:5000 from web browser.
